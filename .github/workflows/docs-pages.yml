name: Deploy documentation to GitHub Pages

on:
  push:
    branches:
      - main
      - master
    paths:
      - ".github/workflows/docs-pages.yml"
      - "README.md"
      - "docs/**"
      - "examples/todo/**"
      - "lattice/**"
      - "lattice.yml"
      - "pyproject.toml"
      - "scripts/build-pages-site.sh"
      - "src/**"
      - "uv.lock"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install uv
        run: python -m pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Validate Lattice docs
        run: uv run lattice validate

      - name: Render Lattice docs
        run: uv run lattice render

      - name: Audit Lattice docs
        run: uv run lattice audit

      - name: Check rendered Lattice docs
        run: uv run lattice render --check

      - name: Validate Todo Lattice
        run: uv run lattice --root examples/todo validate

      - name: Render Todo docs
        run: uv run lattice --root examples/todo render

      - name: Check rendered Todo docs
        run: uv run lattice --root examples/todo render --check

      - name: Assemble Pages site
        run: scripts/build-pages-site.sh

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: pages-site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
